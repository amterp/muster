{
  "concept": "config",
  "source": "authored",
  "citation": "README.md desiderata (AI-native surface: configuration is files) and docs/architecture.md, the shape (config lives in the core). The format and every refusal here are Muster's own policy; there is no oracle above them.",
  "why": "This is the file a person types, and almost everything that can go wrong in it goes wrong silently. A daemon that never appears looks exactly like a daemon that is down, and a misspelled key looks exactly like a key that did nothing - so half of these cases are about what a refusal says rather than about what parses. The refusals are expected in full, not as codes, because the sentence is the deliverable: a taxonomy of error kinds would let the wording rot while every case still passed. Two policies are pinned here and are worth stating outright. A file is applied whole or not at all, because keeping the daemons that parsed would make a window's contents depend on which line had the typo. And an unknown key is refused rather than ignored, which is the opposite of what herdr's own socket API does - ignoring unknown params is why a `pane_id` misspelled for `target_pane_id` silently splits the wrong pane (docs/observations/herdr-0.8.0.md section 6).",
  "cases": [
    {
      "name": "a file with nothing in it names no daemons",
      "why": "An empty config is not an error, and will be the ordinary case the day this file holds only a keymap. It has to be distinguishable from a file that failed to parse, because the caller's answer to those differs: one means find the local daemon, the other means find the local daemon and say loudly that a file was ignored.",
      "given": {
        "file": []
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "never",
        "text": []
      }
    },
    {
      "name": "a daemon needs only a name",
      "why": "Someone running one herdr should have to say one thing. The socket is absent rather than defaulted here, because what gets written down is the wish - find whatever is on this machine - and a path found today is worth nothing to a run tomorrow (architecture.md, durability: persist intent, never observation).",
      "given": {
        "file": [
          "[[daemon]]",
          "id = \"local\""
        ]
      },
      "expect": {
        "daemons": [
          "local local"
        ],
        "keymap": [],
        "option_as_alt": "never",
        "text": []
      }
    },
    {
      "name": "a socket path names one daemon out of several on a machine",
      "why": "Two herdr daemons on one machine differ only by socket path, so this is the only way to say which. It is also the escape hatch for a daemon started with HERDR_SOCKET_PATH, which Muster's own discovery would otherwise have to be running under to find.",
      "given": {
        "file": [
          "[[daemon]]",
          "id = \"scratch\"",
          "socket = \"/tmp/herdr-scratch.sock\""
        ]
      },
      "expect": {
        "daemons": [
          "scratch local=/tmp/herdr-scratch.sock"
        ],
        "keymap": [],
        "option_as_alt": "never",
        "text": []
      }
    },
    {
      "name": "a host makes a daemon remote",
      "why": "Presence of `host` is the whole of local-versus-remote, and it is a destination ssh accepts rather than a hostname Muster parses. Everything about how to reach that destination - port, user, key, ProxyJump - belongs in the user's own ~/.ssh/config, where it already is for every other tool.",
      "given": {
        "file": [
          "[[daemon]]",
          "id = \"devenv\"",
          "host = \"devenv\""
        ]
      },
      "expect": {
        "daemons": [
          "devenv ssh=devenv"
        ],
        "keymap": [],
        "option_as_alt": "never",
        "text": []
      }
    },
    {
      "name": "ssh options are carried verbatim, for what a host alias cannot cover",
      "why": "The escape hatch, and the reason the test fixture works: the devenv container's key lives in the repo rather than in anybody's ssh config. Passed through untouched because the moment Muster starts interpreting these it owns a worse copy of ssh's own option parsing.",
      "given": {
        "file": [
          "[[daemon]]",
          "id = \"devenv\"",
          "host = \"dev@localhost\"",
          "socket = \"/home/dev/.config/herdr/herdr.sock\"",
          "ssh_options = [\"-p\", \"2222\", \"-i\", \"devenv/.ssh/id_ed25519\"]"
        ]
      },
      "expect": {
        "daemons": [
          "devenv ssh=dev@localhost socket=/home/dev/.config/herdr/herdr.sock options=-p 2222 -i devenv/.ssh/id_ed25519"
        ],
        "keymap": [],
        "option_as_alt": "never",
        "text": []
      }
    },
    {
      "name": "the order daemons are listed in is kept",
      "why": "Regions are laid out in the order composition holds them and Muster owns no tree over them, so the file's order is the window's order left to right. A map keyed by name would lose it, and there would be nothing else to recover it from.",
      "given": {
        "file": [
          "[[daemon]]",
          "id = \"local\"",
          "",
          "[[daemon]]",
          "id = \"devenv\"",
          "host = \"devenv\""
        ]
      },
      "expect": {
        "daemons": [
          "local local",
          "devenv ssh=devenv"
        ],
        "keymap": [],
        "option_as_alt": "never",
        "text": []
      }
    },
    {
      "name": "a misspelled key is named rather than skipped",
      "why": "The case this whole policy exists for. `hosts` for `host` produces a daemon that is silently local, which then fails to hold any of the panes the user expected, at a distance of several minutes from the typo. Refusing names the key while the file is still in front of them.",
      "given": {
        "file": [
          "[[daemon]]",
          "id = \"devenv\"",
          "hosts = \"devenv\""
        ]
      },
      "expect": {
        "refused": "the first `[[daemon]]` block has a key called \"hosts\", which Muster does not read. None of the file was applied, because a key nobody reads is usually a misspelling of one somebody meant. Known keys here: id, socket, host, ssh_options."
      }
    },
    {
      "name": "a keymap rebinds what it names",
      "why": "Keyed by action rather than by chord, because an action has one chord and a menu item has one key equivalent - and because `split_right = \"cmd+d\"` is the direction somebody thinks in when they open the file to change a key they did not like.",
      "given": {
        "file": [
          "[keymap]",
          "split_right = \"ctrl+backslash\""
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [
          "split_right=control+Backslash"
        ],
        "option_as_alt": "never",
        "text": []
      }
    },
    {
      "name": "a keymap that names nothing leaves everything where it was",
      "why": "Partial by design: a file that had to restate all fifteen bindings to change one is a file nobody edits twice, and it would silently drop an action the day Muster grows a sixteenth.",
      "given": {
        "file": [
          "[keymap]",
          "zoom = \"cmd+shift+z\""
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [
          "zoom=shift+super+KeyZ"
        ],
        "option_as_alt": "never",
        "text": []
      }
    },
    {
      "name": "an empty chord unbinds the action outright",
      "why": "Different from not mentioning it. Somebody who wants their chord back for something else has to be able to say so, and the alternative is binding it to a key nobody presses and hoping.",
      "given": {
        "file": [
          "[keymap]",
          "close_pane = \"\""
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [
          "close_pane=(unbound)"
        ],
        "option_as_alt": "never",
        "text": []
      }
    },
    {
      "name": "an action Muster does not have",
      "why": "Almost always a misspelling of one it does, so the refusal lists them. Refusing the whole file rather than that line, on the same terms as every other unknown key here: a config half-applied is a config nobody can reason about.",
      "given": {
        "file": [
          "[keymap]",
          "splitt_right = \"cmd+d\""
        ]
      },
      "expect": {
        "refused": "`splitt_right` in the config file's [keymap] is not something Muster does, so none of the file was applied. What it does: new_tab, next_tab, previous_tab, focus_tab_1, focus_tab_2, focus_tab_3, focus_tab_4, focus_tab_5, focus_tab_6, focus_tab_7, focus_tab_8, focus_tab_9, split_right, split_down, split_left, split_up, close_pane, next_pane, previous_pane, focus_left, focus_right, focus_up, focus_down, resize_left, resize_right, resize_up, resize_down, zoom, increase_font_size, decrease_font_size, reset_font_size, toggle_sidebar, show_shortcuts."
      }
    },
    {
      "name": "a chord Muster cannot read",
      "why": "The other half of the same failure, and the one where naming the piece matters most - the refusal has to say which chord and what was wrong with it, or the answer is re-reading the whole file.",
      "given": {
        "file": [
          "[keymap]",
          "zoom = \"cmd+wat\""
        ]
      },
      "expect": {
        "refused": "the config file binds `zoom` to `cmd+wat`, which Muster cannot read: `wat` is not a key Muster knows. Letters and digits are themselves, and the rest are spelled out: left, right, up, down, enter, tab, space, escape, backspace, delete, home, end, pageup, pagedown, f1 to f12. None of the file was applied."
      }
    },
    {
      "name": "one bracket instead of two is the mistake worth catching by name",
      "why": "`[daemon]` and `[[daemon]]` differ by one character and mean a table versus a list of them, and TOML accepts both. Without this the file parses, names no daemons, and looks exactly like an empty config.",
      "given": {
        "file": [
          "[daemon]",
          "id = \"local\""
        ]
      },
      "expect": {
        "refused": "the config file's `daemon` is not a list of blocks, so no daemon in it was applied. Each one is its own `[[daemon]]` block, with the double brackets - a single `[daemon]` declares one table and is the usual way to arrive here."
      }
    },
    {
      "name": "a daemon with no name has nothing to be referred to by",
      "why": "The id is not a fact about the daemon - herdr has no notion of its own identity - it is the name a region, a log line and a pane are attributed to. A daemon without one cannot be talked about, so there is no useful thing to do with the block.",
      "given": {
        "file": [
          "[[daemon]]",
          "host = \"devenv\""
        ]
      },
      "expect": {
        "refused": "the first `[[daemon]]` block has no `id`, so there is no name to refer to it by and none of the file was applied. An id is Muster's own name for a daemon rather than anything the daemon knows about itself - `local` and `devenv` read well in a log line, which is where they are met."
      }
    },
    {
      "name": "two daemons cannot share a name, and the refusal says which two",
      "why": "Names are how everything downstream tells daemons apart, including the pane ids that collide between them. Saying only that a name repeats leaves someone scrolling a file to find the second one, so the refusal describes both.",
      "given": {
        "file": [
          "[[daemon]]",
          "id = \"work\"",
          "",
          "[[daemon]]",
          "id = \"work\"",
          "host = \"devenv\""
        ]
      },
      "expect": {
        "refused": "two daemons are called `work`, and a name is how everything else refers to one - a region, a log line, a pane that belongs to it. None of the file was applied. Rename one: the first is a daemon on this machine, the second is a daemon on devenv."
      }
    },
    {
      "name": "ssh options with no host describe something that does not exist",
      "why": "A local daemon reached over ssh is not a thing, and the likely intent is a `host` line that was deleted or never typed. Silently ignoring the options would attach a local daemon under a name the user expects to be remote.",
      "given": {
        "file": [
          "[[daemon]]",
          "id = \"devenv\"",
          "ssh_options = [\"-p\", \"2222\"]"
        ]
      },
      "expect": {
        "refused": "the first `[[daemon]]` block sets `ssh_options` and no `host`, so it describes a daemon on this machine reached over ssh, which is not a thing. None of the file was applied. Add the `host` this was meant for, or drop the options."
      }
    },
    {
      "name": "a value of the wrong type says what it found",
      "why": "TOML makes 2222 a number and \"2222\" a string, and a port typed without quotes is the way into this. Naming what was found rather than only what was wanted is the difference between one edit and a guess.",
      "given": {
        "file": [
          "[[daemon]]",
          "id = \"devenv\"",
          "host = 2222"
        ]
      },
      "expect": {
        "refused": "the first `[[daemon]]` block sets `host` to a number, and it has to be a string. None of the file was applied."
      }
    },
    {
      "name": "the block that is wrong is the block that is named",
      "why": "A file with several daemons in it has several places a key can be wrong, and only one of them is. The ordinal is how someone finds it without counting blocks themselves.",
      "given": {
        "file": [
          "[[daemon]]",
          "id = \"local\"",
          "",
          "[[daemon]]",
          "id = \"devenv\"",
          "host = \"devenv\"",
          "",
          "[[daemon]]",
          "id = \"other\"",
          "sock = \"/tmp/herdr.sock\""
        ]
      },
      "expect": {
        "refused": "the third `[[daemon]]` block has a key called \"sock\", which Muster does not read. None of the file was applied, because a key nobody reads is usually a misspelling of one somebody meant. Known keys here: id, socket, host, ssh_options."
      }
    },
    {
      "name": "option composes characters unless the file says otherwise",
      "why": "macOS's own behavior is the default, because it is what somebody typing accented text expects and Muster has no way to know which kind of typist it has. Stated as a case rather than assumed, since the cost of the default is that every alt chord an agent binds is dead until this key is set.",
      "given": {
        "file": []
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "never",
        "text": []
      }
    },
    {
      "name": "option_as_alt names a side in the words a person writes",
      "why": "`left` rather than `leftOnly`: the wire's spelling is right for a corpus and wrong for a hand-edited file, the same split the chord grammar already makes between `cmd` and `super`. The per-side values are the ones people actually pick, because option composes on macOS and a common arrangement is one side for meta chords with the other still composing.",
      "given": {
        "file": [
          "option_as_alt = \"left\""
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "leftOnly",
        "text": []
      }
    },
    {
      "name": "the wire's own spelling is read too",
      "why": "A name copied out of a log line or a conformance case has to work in the file, or the two vocabularies drift and nobody can tell which one they are looking at.",
      "given": {
        "file": [
          "option_as_alt = \"rightOnly\""
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "rightOnly",
        "text": []
      }
    },
    {
      "name": "an option_as_alt Muster does not know says what it takes",
      "why": "Silently defaulting to `never` here would produce exactly the symptom this key exists to fix, with a file on disk that looks like it fixed it. The refusal quotes the four names rather than saying `invalid value`, because the mistake is nearly always a near-miss.",
      "given": {
        "file": [
          "option_as_alt = \"leftonly\""
        ]
      },
      "expect": {
        "refused": "`option_as_alt` in the config file is \"leftonly\", which Muster does not know, so none of the file was applied. It is one of `never`, `always`, `left`, `right`: `never` leaves option composing characters the way macOS does, and the others make that side of the keyboard send alt instead."
      }
    },
    {
      "name": "a chord can stand for literal bytes",
      "why": "The distinction an agent needs most and a terminal cannot always carry: shift+enter as a newline rather than as a carriage return. `[text]` is keyed by chord where `[keymap]` is keyed by action, and deliberately - an action has one chord so its name reads best, and text has no name at all, so the chord is the only thing left to key on.",
      "given": {
        "file": [
          "[text]",
          "\"shift+enter\" = \"\\n\""
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "never",
        "text": [
          "shift+Enter=0a"
        ]
      }
    },
    {
      "name": "text bindings are read in a stable order",
      "why": "Two runs over the same file must build the same keymap. A hash map over chords gives no such promise, and the day two bindings collide the winner would depend on the run rather than on the file.",
      "given": {
        "file": [
          "[text]",
          "\"ctrl+g\" = \"hello\"",
          "\"shift+enter\" = \"\\n\""
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "never",
        "text": [
          "control+KeyG=68656c6c6f",
          "shift+Enter=0a"
        ]
      }
    },
    {
      "name": "a text chord Muster cannot read is named rather than skipped",
      "why": "The same policy the keymap already follows. A chord that silently did nothing is indistinguishable from a chord that is bound correctly and whose bytes went nowhere, and only one of those is the user's mistake to fix.",
      "given": {
        "file": [
          "[text]",
          "\"shift+enterr\" = \"\\n\""
        ]
      },
      "expect": {
        "refused": "the config file's [text] has a chord `shift+enterr` which Muster cannot read: `enterr` is not a key Muster knows. Letters and digits are themselves, and the rest are spelled out: left, right, up, down, enter, tab, space, escape, backspace, delete, home, end, pageup, pagedown, f1 to f12. None of the file was applied."
      }
    },
    {
      "name": "a text chord bound to nothing is refused rather than read as an unbinding",
      "why": "`[keymap]` reads an empty chord as `unbind this action`, and the symmetry is tempting and wrong. There is nothing here to unbind: every default text binding is a chord no program asks for, so a file that wants one back binds it to the bytes it prefers. Refusing keeps the day that changes an explicit decision rather than a silent one already made.",
      "given": {
        "file": [
          "[text]",
          "\"shift+enter\" = \"\""
        ]
      },
      "expect": {
        "refused": "the config file's [text] gives `shift+enter` an empty string, so pressing it would send nothing at all. None of the file was applied. Delete the line to leave the chord alone."
      }
    },
    {
      "name": "two spellings of one chord cannot both be bound",
      "why": "`shift+enter` and `enter+shift` are the same chord and TOML sees two keys, so one silently wins. Which one depends on the table's order, which is not something a person reading their own file can work out.",
      "given": {
        "file": [
          "[text]",
          "\"shift+enter\" = \"\\n\"",
          "\"enter+shift\" = \"x\""
        ]
      },
      "expect": {
        "refused": "the config file's [text] binds `shift+enter` twice under different spellings, and one of them silently wins. None of the file was applied."
      }
    },
    {
      "name": "the two knobs, each read as itself",
      "why": "A resize step in cells and a scroll multiplier that scales whatever the input device reported. Grouped in one case because they share nothing but a shape - each is a single value with a defensible default - and separate cases would be two copies of the same assertion. The divider colour used to sit beside them and now sits with the other colours, because somebody picking colours picks all of them at once.",
      "given": {
        "file": [
          "resize_step = 2",
          "scroll_multiplier = 1.5"
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "never",
        "text": [],
        "feel": [
          "resize_step=2",
          "scroll_multiplier=1.5"
        ]
      }
    },
    {
      "name": "a file that names none of them leaves every default alone",
      "why": "The overwhelmingly common case, and the reason these are absent from every other case in this file. The defaults are chosen so that a file naming nothing behaves exactly as Muster did before the keys existed: the daemon's own resize step, the device's own scroll delta, and - for every appearance value - whatever the renderer would have painted anyway. Muster names no appearance default of its own, so this case is also what pins that.",
      "given": {
        "file": [
          "[[daemon]]",
          "id = \"local\""
        ]
      },
      "expect": {
        "daemons": [
          "local local"
        ],
        "keymap": [],
        "option_as_alt": "never",
        "text": []
      }
    },
    {
      "name": "a whole number and a decimal are the same number",
      "why": "TOML tells an integer from a float and nobody writing `resize_step = 2` means anything different from `2.0`. Refusing the integer would be a parser detail leaking into a file somebody hand-edits, and the error would say 'has to be a number' about a number.",
      "given": {
        "file": [
          "resize_step = 2",
          "scroll_multiplier = 2"
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "never",
        "text": [],
        "feel": [
          "resize_step=2",
          "scroll_multiplier=2"
        ]
      }
    },
    {
      "name": "a colour may be written with or without its hash",
      "why": "Both are what somebody types, and a file that silently ignored one spelling would be a colour that does not apply for a reason nobody can see. Normalized on the way out so the shell has one form to read, whichever colour it was.",
      "given": {
        "file": [
          "[colors]",
          "background = \"4a4a4a\"",
          "divider = \"#FFFFFF\""
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "never",
        "text": [],
        "appearance": [
          "colors.background=#4a4a4a",
          "colors.divider=#ffffff"
        ]
      }
    },
    {
      "name": "a resize step of zero is refused, because a resize has to move",
      "why": "Zero is what proto3 uses for a field nobody set, so a zero reaching the seam would silently mean 'the daemon's own step' - the file would say one thing and the window would do another. Refusing here means the only way to get the daemon's step is to not name one, which is one answer rather than two spellings of it. The refusal quotes the value rather than its type: `is a number, and it has to be a whole number` reads like a bug in Muster.",
      "given": {
        "file": [
          "resize_step = 0"
        ]
      },
      "expect": {
        "refused": "`resize_step` in the config file is 0, and it has to be a whole number of cells, at least one. None of the file was applied. Leave it out to keep the daemon's own step, which is what a chord meant before this key existed."
      }
    },
    {
      "name": "a scroll multiplier of zero is refused, because the wheel would do nothing",
      "why": "A wheel that scrolls nowhere is indistinguishable from a broken wheel, and somebody who set this to zero would go looking for the bug in Muster rather than in their file. Negatives are refused with it: inverted scrolling is a real preference, but it belongs to the direction rather than to the magnitude, and smuggling it into a multiplier would make `-1` mean something no other key in this file means.",
      "given": {
        "file": [
          "scroll_multiplier = 0"
        ]
      },
      "expect": {
        "refused": "`scroll_multiplier` in the config file is 0, so the wheel would scroll nowhere or backwards. None of the file was applied. It scales what the input device reports, so 1 is the device's own answer, 2 is twice as far, and 0.5 is half."
      }
    },
    {
      "name": "a colour that is not six hex digits is refused where it was written",
      "why": "The alternative is a shell handed `#gg0000`, which can only fail silently or invent something - both a long way from the person who typed it. Reading the file is where the line number is, so it is where the refusal belongs. The key is named as well as the value, because a file setting eight colours has eight places this could be.",
      "given": {
        "file": [
          "[colors]",
          "background = \"#gg0000\""
        ]
      },
      "expect": {
        "refused": "`background` in the config file's [colors]: `#gg0000` is not a colour Muster can read. Write six hex digits, with or without a leading hash - `#4a4a4a`. None of the file was applied."
      }
    },
    {
      "name": "a knob written as a string is refused rather than parsed",
      "why": "TOML has numbers and this is one, so accepting `\"2\"` would be Muster inventing a second spelling for something the format already has. Saying so names the key, which is the whole job of this refusal.",
      "given": {
        "file": [
          "resize_step = \"2\""
        ]
      },
      "expect": {
        "refused": "`resize_step` in the config file is a string, and it has to be a whole number of cells, at least one. None of the file was applied. Leave it out to keep the daemon's own step, which is what a chord meant before this key existed."
      }
    },
    {
      "name": "a resize step with a fraction in it is refused",
      "why": "A chord cannot mean half a cell. `ResizePane.amount` is a float at the seam so a CLI can place a divider exactly, but that is a different question from how far one keypress moves - and accepting `1.5` here would be a number the terminal has nowhere to put, silently rounded by whichever layer noticed first.",
      "given": {
        "file": [
          "resize_step = 1.5"
        ]
      },
      "expect": {
        "refused": "`resize_step` in the config file is 1.5, and it has to be a whole number of cells, at least one. None of the file was applied. Leave it out to keep the daemon's own step, which is what a chord meant before this key existed."
      }
    },
    {
      "name": "every appearance key, each read as itself",
      "why": "The whole vocabulary in one file, which is what somebody who has decided to make Muster look like their terminal actually writes. One case rather than fifteen because the assertion is identical for each and the interesting question is whether they coexist: three blocks and a root key, parsed together, none of them clobbering another. `hollow` surviving as `hollow` is the part worth watching - it is Muster's word for the shape, and a renderer that calls it something else translates in the shell.",
      "given": {
        "file": [
          "pane_padding = 4",
          "",
          "[font]",
          "family = \"Fira Code\"",
          "size = 13",
          "",
          "[colors]",
          "background = \"#282c34\"",
          "foreground = \"#ffffff\"",
          "cursor = \"#f5e0dc\"",
          "cursor_text = \"#1e1e2e\"",
          "selection_background = \"#414868\"",
          "selection_foreground = \"#c0caf5\"",
          "divider = \"#4a4a4a\"",
          "",
          "[cursor]",
          "style = \"hollow\"",
          "blink = false"
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "never",
        "text": [],
        "appearance": [
          "font.family=Fira Code",
          "font.size=13",
          "colors.background=#282c34",
          "colors.foreground=#ffffff",
          "colors.cursor=#f5e0dc",
          "colors.cursor_text=#1e1e2e",
          "colors.selection_background=#414868",
          "colors.selection_foreground=#c0caf5",
          "colors.divider=#4a4a4a",
          "cursor.style=hollow",
          "cursor.blink=false",
          "pane_padding=4"
        ]
      }
    },
    {
      "name": "the sixteen ANSI colours are a set, and read as one",
      "why": "The palette is the primitive a theme is made of, and Muster has no theme format of its own - naming one would mean adopting a file format belonging to whichever renderer is behind the seam. Sixteen rather than 256 because sixteen is what people write by hand and the rest are generated.",
      "given": {
        "file": [
          "[colors]",
          "palette = [",
          "  \"#000000\", \"#cc0000\", \"#4e9a06\", \"#c4a000\",",
          "  \"#3465a4\", \"#75507b\", \"#06989a\", \"#d3d7cf\",",
          "  \"#555753\", \"#ef2929\", \"#8ae234\", \"#fce94f\",",
          "  \"#729fcf\", \"#ad7fa8\", \"#34e2e2\", \"#eeeeec\",",
          "]"
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "never",
        "text": [],
        "appearance": [
          "colors.palette=#000000 #cc0000 #4e9a06 #c4a000 #3465a4 #75507b #06989a #d3d7cf #555753 #ef2929 #8ae234 #fce94f #729fcf #ad7fa8 #34e2e2 #eeeeec"
        ]
      }
    },
    {
      "name": "no padding at all is a thing somebody asks for",
      "why": "Zero is a real answer here where it is a refusal for `resize_step`, and the difference is worth pinning because the two look alike. A resize that moves nothing is a broken chord; a pane with no padding is a window that fits more rows, which is exactly what somebody watching fifteen agents wants.",
      "given": {
        "file": [
          "pane_padding = 0"
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "never",
        "text": [],
        "appearance": [
          "pane_padding=0"
        ]
      }
    },
    {
      "name": "an empty font family is how you say \"whatever this machine has\"",
      "why": "Treated as absent rather than refused. Somebody commenting out their font by emptying the string means the renderer's own, and refusing the whole file for it would be a refusal about nothing. It is the one appearance value where the machine, not the person, has the better answer - a family nobody installed is worse than no answer at all.",
      "given": {
        "file": [
          "[font]",
          "family = \"\"",
          "size = 14"
        ]
      },
      "expect": {
        "daemons": [],
        "keymap": [],
        "option_as_alt": "never",
        "text": [],
        "appearance": [
          "font.size=14"
        ]
      }
    },
    {
      "name": "the colour that moved is refused by name at the root",
      "why": "`divider_color` was a root key until the colours got a block of their own, so a file written against an older Muster names it there. The unknown-key refusal already covers this, and this case pins that it does: the sentence names the key somebody has to move and lists where the keys now live. Without it the symptom is a window with no daemons and no explanation.",
      "given": {
        "file": [
          "divider_color = \"#4a4a4a\""
        ]
      },
      "expect": {
        "refused": "the config file has a key called \"divider_color\", which Muster does not read. None of the file was applied, because a key nobody reads is usually a misspelling of one somebody meant. Known keys here: daemon, keymap, text, option_as_alt, resize_step, scroll_multiplier, pane_padding, font, colors, cursor."
      }
    },
    {
      "name": "a misspelled colour is refused against the block it was written in",
      "why": "A file with three appearance blocks has three places a key can be wrong, and a refusal naming only the key would send somebody to the wrong one. This is also what makes the block listing useful: `backgrnd` is close enough to `background` that seeing the list is the whole fix.",
      "given": {
        "file": [
          "[colors]",
          "backgrnd = \"#282c34\""
        ]
      },
      "expect": {
        "refused": "the config file's [colors] has a key called \"backgrnd\", which Muster does not read. None of the file was applied, because a key nobody reads is usually a misspelling of one somebody meant. Known keys here: background, foreground, cursor, cursor_text, selection_background, selection_foreground, divider, palette."
      }
    },
    {
      "name": "a font size outside what a renderer will take is refused here",
      "why": "libghostty clamps this to 1..255 and says nothing, so a `size = 0` would silently become 1 - a window nobody can read with no line explaining why. Refusing at the point the file is read is the only place the person who typed it is still in the picture. The range is the renderer's, which is a borrowed number and worth knowing is borrowed.",
      "given": {
        "file": [
          "[font]",
          "size = 0"
        ]
      },
      "expect": {
        "refused": "`size` in the config file's [font] is 0, and it has to be a number of points between 1 and 255. None of the file was applied."
      }
    },
    {
      "name": "a font size written as a string is refused as a number would be",
      "why": "TOML has numbers and this is one, matching the same refusal for `resize_step`. Worth its own case because the sentence has to name the block: `size` is a key two blocks could plausibly have, and the file being refused whole means the reader has only this sentence to go on.",
      "given": {
        "file": [
          "[font]",
          "size = \"13\""
        ]
      },
      "expect": {
        "refused": "`size` in the config file's [font] is a string, and it has to be a number. None of the file was applied."
      }
    },
    {
      "name": "a cursor shape Muster does not know is refused with the list",
      "why": "The four shapes are a closed set and the names are not guessable - `hollow` rather than `block_hollow`, and no `beam` even though half the terminals in the world call it that. So the refusal carries the whole list rather than saying the value was wrong.",
      "given": {
        "file": [
          "[cursor]",
          "style = \"beam\""
        ]
      },
      "expect": {
        "refused": "`style` in the config file's [cursor] is \"beam\", which is not a shape Muster knows, so none of the file was applied. It is one of `block`, `bar`, `underline`, `hollow`."
      }
    },
    {
      "name": "a blink that is not true or false is refused, and says what absent means",
      "why": "Three states hide in this key - on, off, and let the program in the pane decide - and only two of them are values. Somebody reaching for a third spelling needs telling that the third state is the absent one, or they will invent `blink = \"auto\"` and get a refused file.",
      "given": {
        "file": [
          "[cursor]",
          "blink = \"yes\""
        ]
      },
      "expect": {
        "refused": "`blink` in the config file's [cursor] is a string, and it has to be true or false. None of the file was applied. Leave it out to let the program in the pane decide, which is what it did before this key existed."
      }
    },
    {
      "name": "a palette that is not sixteen colours is refused",
      "why": "A partial palette is the failure that would look like it worked: four of somebody's colours over twelve of the renderer's renders, and the result is a scheme nobody designed rather than an error. It is also indistinguishable from a file somebody stopped editing halfway, which is the likelier cause.",
      "given": {
        "file": [
          "[colors]",
          "palette = [\"#000000\", \"#cc0000\", \"#4e9a06\"]"
        ]
      },
      "expect": {
        "refused": "`palette` in the config file's [colors] has 3 entries, and it has to have exactly 16 - one for each ANSI colour, black through bright white. None of the file was applied. A palette is a set rather than a list of separate choices, so a partial one would leave the rest as the renderer's and produce a scheme nobody designed."
      }
    },
    {
      "name": "a palette entry that is not a colour says which entry",
      "why": "Sixteen near-identical strings on one line, and a refusal saying only that one of them is wrong would leave somebody counting hex digits. The position is the whole of what makes this actionable.",
      "given": {
        "file": [
          "[colors]",
          "palette = [",
          "  \"#000000\", \"#cc0000\", \"nope\", \"#c4a000\",",
          "  \"#3465a4\", \"#75507b\", \"#06989a\", \"#d3d7cf\",",
          "  \"#555753\", \"#ef2929\", \"#8ae234\", \"#fce94f\",",
          "  \"#729fcf\", \"#ad7fa8\", \"#34e2e2\", \"#eeeeec\",",
          "]"
        ]
      },
      "expect": {
        "refused": "the third entry of `palette` in the config file's [colors]: `nope` is not a colour Muster can read. Write six hex digits, with or without a leading hash - `#4a4a4a`. None of the file was applied."
      }
    },
    {
      "name": "an appearance block written as a value is refused with its keys",
      "why": "`colors = \"dark\"` is what somebody expecting a theme name writes, and it is a reasonable thing to expect - most terminals have one. Muster does not, so the refusal has to do two jobs: say this is a block, and list what goes in it, because the answer to \"then how do I set a theme\" is the palette and the six colours.",
      "given": {
        "file": [
          "colors = \"dark\""
        ]
      },
      "expect": {
        "refused": "`colors` in the config file is a string, and it has to be a block - `[colors]` with its settings under it. None of the file was applied. Known keys there: background, foreground, cursor, cursor_text, selection_background, selection_foreground, divider, palette."
      }
    },
    {
      "name": "a padding with a fraction in it is refused",
      "why": "Whole points, matching `resize_step`'s whole cells. A renderer takes an integer here and would round it, so accepting `1.5` means the file says one thing and the window does another.",
      "given": {
        "file": [
          "pane_padding = 1.5"
        ]
      },
      "expect": {
        "refused": "`pane_padding` in the config file is 1.5, and it has to be a whole number of points, zero or more. None of the file was applied. Zero is the one that fits the most rows in a window; leave it out for the renderer's own answer."
      }
    }
  ]
}
