{
  "_v": 3,
  "alias": "the-suite-tests",
  "alias_explicit": false,
  "area": [
    "testing"
  ],
  "column": "next",
  "comments": [
    {
      "author": "Alexander Terp",
      "body": "Two reviews landed independently on this card as the biggest hole, and both named the same cheapest way in.\n\nThe gap is precise: every layer of a gesture is tested and the composed gesture is not. crates/muster-seam/tests/ hits a real daemon end to end but builds protobuf requests in Rust, bypassing Swift's event translation and the FFI marshalling entirely; Tests/MusterMacTests/SeamTests.swift crosses the real FFI boundary but with no daemon behind it; tools/smoke-launch.py runs the real binary against a real daemon and says of itself 'No keyboard needed, so it says nothing about what typing does'. Nothing joins the three.\n\nCheapest way in, per both: extend --contract rather than build a tier. It already has the real app, a real daemon and a run log to read - it just stops at 'did it connect'. Sending one real intent through the app's own surface and asserting the daemon's pane content changed reuses all of that.\n\nTwo specific holes worth naming as the first cases:\n- ArrangePane (drag-to-swap) has no real-daemon gesture test at all, unlike focus_pane_at which has one in numbering.rs. Its pieces are tested separately - SidebarTests checks drop legality, backend-intent.json checks wire encoding - and nothing dispatches a real one and checks the roster moved.\n- No test runs two live daemons. roster.json proves the cross-daemon numbering maths on synthetic data; numbering.rs proves the gesture against one daemon. Multi-daemon routing under two real connections is the whole 'local and remote in one window' desideratum and is untested.\n\nAlso worth recording here: the seam's process globals limit these files to one #[test] each, and the real cost is bisectability rather than coverage - attach.rs chains nine sub-scenarios through helpers, so a failure partway through aborts before the rest run, and one red CI run cannot tell you whether the later behaviour also broke.",
      "created_at_millis": 1786929150022,
      "id": "c_29LPLOHgf"
    }
  ],
  "created_at_millis": 1786899262089,
  "creator": "Alexander Terp",
  "description": "Three bugs shipped green on this branch and all three were found by launching the app and looking at it. None of them needed a GUI to catch, and that is the finding: the suite has no tier that runs a whole gesture end to end and asserts on what the window would be told.\n\n    a stale name put back by a replayed event      mirror + subscription, each tested alone\n    a tab caption blanked by a replayed creation   same, and it rendered blank rather than wrong\n    a rename that never reached its own window     request, envelope and drawing all tested; the answer was applied by nobody\n\nThey share a shape. Every layer each one crosses is tested, and the **composition** of those layers is not. The third is the clearest: the Swift side asserts the request it builds, the core asserts the intent it dispatches, `backend-intent.json` asserts the herdr envelope, and the roster's own cases assert that a name is drawn - and between the daemon's reply and the mirror there was no code at all, so there was nothing for any of them to be wrong about.\n\n**`crates/muster-seam/tests/attach.rs` is already this tier and has not grown.** It runs a real daemon, drives it with encoded protobuf `Request`s - the exact bytes the shell sends - and asserts on emitted `Event`s, which are the exact bytes the shell renders. That is the window's contract without a window. \"Send RenamePane, assert the next RosterChanged carries the name\" is one test in that file and would have caught the third bug the moment it was written.\n\nWhat stops it growing is written at the top of the file: the seam holds its session in a process global, so it is one test per binary. Every gesture costs a new file and a new daemon, which is why one file covers splits and nothing covers anything else. Fixing that - a reset between tests, or a session handle the tests can own - is the change that turns a tier that exists into a tier people add to.\n\nThe reconnect case is the other half and needs no new mechanism, only a test that does the thing: kill the connection under a live mirror, let it come back, and assert the mirror still holds what it held. Both replay bugs are that test. `docs/observations/herdr-0.8.0.md` section 16 explains why the replay is hazardous; nothing exercises it.\n\n**What genuinely cannot move here**, and should stay small and manual: pixels, `NSTextField` behaviour, where a sheet lands, whether a row is legible. `./dev --contract` already launches the real app against a real herdr and reads its run log, and it currently asserts only what connected - there is room to assert far more there once the muster CLI can drive a running window.\n\nWorth deciding as part of this: whether the run log becomes an assertion surface on purpose. `architecture.md` already says it is meant to be one, and `roster.published` carries the counts a sidebar bug would show up in.",
  "id": "a_298rp2Pss",
  "position": "!0",
  "title": "🧪 The suite tests every layer of a gesture and never the gesture",
  "type": "chore",
  "updated_at_millis": 1786929150022,
  "history": [
    {"field":"column","value":"backlog-lo","at":1786899262089},
    {"field":"column","value":"next","at":1786899266495}
  ]
}