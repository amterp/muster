{
  "_v": 3,
  "alias": "stage-3-ghostty",
  "alias_explicit": false,
  "area": [
    "shell"
  ],
  "column": "next",
  "comments": [
    {
      "author": "Alexander Terp",
      "body": "From the developer: \"I have some custom ghostty keybindings under ~/.config/ghostty/config, I will want those for muster.\"\n\nThat config, read 2026-08-15:\n\n    cmd+shift+alt+h/j/k/l   new_split:left / down / up / right\n    cmd+[ / cmd+]           goto_split:previous / next\n    cmd+shift+h/j/k/l       resize_split:left/down/up/right,150\n    shift+enter             text:\\n\n\nMuster already matches `cmd+[` and `cmd+]` exactly, and `shift+enter` maps onto the SendText message that exists. The rest turns out not to be a config-format problem - three of these name actions Muster has no way to express at all:\n\n- **Splitting left or up.** SplitPane says columns or rows and the new pane goes beside or below, because herdr's `pane.split` takes `right` or `down` and nothing else. Splitting the other way means putting the new pane first, which is a rearrangement rather than a split. Worth checking whether herdr can move a pane within a tab, and if not this is a fifth upstream ask.\n- **Resizing by a keyboard amount.** SetSplitRatio carries a ratio, because it is built for a drag that knows where the pointer is. `resize_split:left,150` is a nudge of 150 points, which needs either a delta the core turns into a ratio against a size only the shell knows, or a \"nudge\" the shell resolves itself.\n- **Sending literal text on a chord.** Expressible - SendText - but nothing today lets a binding name it.\n\nThe design question the card was already carrying, now with a concrete case: does Muster read ghostty's `keybind` lines, or does its keymap live in Muster's own config? Reading ghostty's has real pull - the renderer already loads that file for fonts and colours (`ghostty_config_load_default_files`), the shortcuts here were deliberately chosen to be Ghostty's, and duplicating a keymap is duplicating the thing most likely to drift. Against it: the actions are ghostty's vocabulary and some of them do not map, so a silently ignored binding would be a dead key - the failure this project has spent the most time on. If they are read, an unmappable one has to say so out loud.\n\narchitecture.md says the keymap is data in the config file rather than code. It does not say whose config file.",
      "created_at_millis": 1786790532798,
      "id": "c_28PFHDBDs"
    }
  ],
  "created_at_millis": 1786461793497,
  "creator": "Alexander Terp",
  "description": "cmd+d splits, cmd+opt+arrow focus, cmd+ctrl+shift+arrow resize (the one even cmux lacks), cmd+shift+return zoom. File-based config; every action rebindable.",
  "id": "a_26BJGXAS9",
  "position": "UUUUUUUUUUUUUUUU",
  "title": "Stage 3: Ghostty-parity keymap, fully rebindable",
  "type": "feature",
  "updated_at_millis": 1786790571081,
  "history": [
    {"field":"column","value":"backlog-hi","at":1786461793497},
    {"field":"column","value":"next","at":1786787768134}
  ]
}