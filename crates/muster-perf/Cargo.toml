[package]
name = "muster-perf"
version.workspace = true
edition.workspace = true
publish.workspace = true

[[bin]]
name = "muster-perf"
path = "src/main.rs"

[dependencies]
libc.workspace = true
muster-core.workspace = true
muster-herdr.workspace = true
muster-seam.workspace = true
muster-vt.workspace = true
serde = { version = "1", features = ["derive"] }
prost.workspace = true
serde_json.workspace = true

# Only the watcher-cost example, which needs a real daemon to hold connections open
# against. The budgets in src/main.rs deliberately spawn nothing.
[dev-dependencies]
herdr-harness.workspace = true

[lints]
workspace = true
